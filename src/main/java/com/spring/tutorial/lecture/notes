1. What is HTTP
    https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview
    - schemas
    -


    + methods
        https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods
    + status codes
        https://developer.mozilla.org/en-US/docs/Web/HTTP/Status
    + headers
        2
    + example messages
    - HTTP2
        https://programmer.help/blogs/java-9-spring-boot-2-undertow2-enables-http2-and-server-push.html
        curl --head http://localhost:8080/api/resource/1
2. What is Restful service
3. What are Controllers
    + examples with resource/{id} for GET, POST, PUT, DELETE
    + resource/{year}/{week}
    + query parameters resource/year=2020&week=12
    + validations
        + resource/{title} @NotNull
        + resource/{percentage} @Min @Max
        + resource/{idsList} @NotEmpty
        + resource/{idsList} @Size
    + Converting enums and dates
        + resource/all by date and CountryEnum
        + resource/all before timestamp
    + RequestBody
        + basics
        + validations
        + RequestBody + RequestParameters
        + with image
    + Returning objects
    + Accessing HttpContext

    SHOW EXAMPLES from k-crator
        - returning pdfs (PdfUtils and PrintController) and images (ImageController)
        https://localhost:9090/api//v1/images/01cb4484-c13d-4845-3cc1-4dff2be04772/4
        - accessing Principal and UserDetails



What happens with duplicated endpoints
How Postman works + Code
Json properties
